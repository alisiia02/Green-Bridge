{
  "name": "green-bridge",
  "private": true,
  "version": "0.1.0",
  "description": "Green Bridge - a pattern language for cross-continental green integration",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c green,cyan \"vite --config frontend/vite.config.ts\" \"tsx watch backend/src/server.ts\"",
    "dev:web": "vite --config frontend/vite.config.ts",
    "dev:api": "tsx watch backend/src/server.ts",
    "build": "cd frontend && tsc --noEmit && vite build && cd ../backend && tsc",
    "build:web": "cd frontend && tsc --noEmit && vite build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
