{
  "name": "green-bridge",
  "private": true,
  "version": "0.1.0",
  "description": "Green Bridge - a pattern language for cross-continental green integration",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c green,cyan \"cd frontend && vite\" \"cd backend && tsx watch src/server.ts\"",
    "dev:web": "cd frontend && vite",
    "dev:api": "cd backend && tsx watch src/server.ts",
    "build": "cd frontend && tsc --noEmit && vite build && cd ../backend && tsc"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
